# セッション引継ぎ — 2026-05-06 23:09 — ID: 434

## 🎯 作業のゴール

`/r` スキル（遠隔指示取り込み）専用 PWA を新規実装する。
Google Tasks 認証が頻繁に切れる問題を解消するため、TeamTasks PWA に相乗りする形で
`r_instructions` テーブル + FastAPI ルータ + Google Todo 風 PWA を作成し、
`/r NNN` で番号指定の単発取り込みを可能にする。

**重要**: 本セッションは Lv4 相談・実装・検証まで完了済み。**ただし作業 PC 違い**で、
TeamTasks の本番サーバが稼働している PC で動作確認・反映する必要がある。

## ✅ やったこと

### Lv4 相談（完了）
- Gemini に 4 案提示させ、Claude 検討
- DeepSeek に「別レイヤー」案を出させた
- Codex で最終レビュー → 6 列統合表で採用方針確定
- ユーザー承認: 「TeamTasks 相乗り型 + Codex 指摘の堅牢化、認証不要、Google Tasks 移行不要、フェーズ1全部実装」

### 実装（完了、ローカル PC 上で）

**既存ファイル変更**（バックアップ済み `.bak_20260506_191856`）:
- `014.TeamTasks/server/models.py` — `RInstruction` / `RInstructionVersion` 追加
- `014.TeamTasks/server/db.py` — PRAGMA WAL / busy_timeout=5000 / synchronous=NORMAL 追加、init_db で R モデル import
- `014.TeamTasks/server/main.py` — `include_router(r_router.router)` 追加
- `014.TeamTasks/server/schemas.py` — `RInstruction*` Pydantic 5 種追加
- `.claude/commands/r.md` — Google Tasks → DB API へ全面書き換え（`/r NNN` 単発 + 一括両対応）

**新規ファイル**:
- `014.TeamTasks/server/routers/r.py` — 7 エンドポイント
  - GET /r/ (一覧, status/archived/limit クエリ)
  - POST /r/ (新規, code 省略時サーバ採番)
  - GET /r/{code} (単発閲覧, versions 込み, 副作用なし)
  - PATCH /r/{code} (編集, 新版 append)
  - POST /r/{code}/consume (CAS 風 UPDATE, 先着 200 / 409 already_consumed)
  - POST /r/{code}/restore (consumed_at = NULL に戻し)
  - POST /r/{code}/archive (archived フラグトグル)
- `014.TeamTasks/pwa/r/index.html` / `app.js` / `style.css` / `manifest.json` / `sw.js`

**その他**:
- `.handoff/r_processed.json` → `.handoff/r_processed.json.frozen_20260506_193016` にリネーム（凍結）

### 検証（ローカル PC 上で完了）
- import 全 OK（models / schemas / r.py / db / main、`/r/*` 7 ルート登録確認）
- E2E TestClient テスト 23 シナリオ全 PASS（CRUD / consume CAS / 409 / 編集版管理 / バリデーション）

### Codex 再レビュー（差分のみ）
- 🔴 1 件: `_generate_code` 設計意図ズレ（後述、未対応）
- 🟠 1 件: 認証なし公開（ユーザー承認済「A. 認証不要」）
- 🟡 1 件: PWA `data-code` 属性未エスケープ（数字制約のため実害なし）
- consume CAS / PATCH 不変 / XSS / WAL / urllib / SW / index — すべて妥当

## 🚧 未完了タスク

### 別 PC で必要な作業（最重要）

- [ ] **本番 TeamTasks サーバの再起動** — port 8086 の uvicorn を再起動して新ルータ反映 + WAL モード適用
  - 再起動コマンド: タスクスケジューラ経由で `start.bat` 再実行、または手動で uvicorn プロセス停止→再起動
  - 起動コマンド例: `"C:\\Users\\user\\AppData\\Local\\Python\\pythoncore-3.14-64\\python.exe" -m uvicorn main:app --host 127.0.0.1 --port 8086`
- [ ] **DB スキーマ反映確認** — 再起動時 `init_db()` の `Base.metadata.create_all` で `r_instructions` / `r_instruction_versions` 自動作成（既存 `tasks.db` 影響なし）
- [ ] **動作確認 E2E** — スマホ Safari で `https://sfuji.f5.si/tasks/r/` 開いて 1 件追加 → PC で `/r 番号` 取り込み
- [ ] **依存パッケージ確認** — 本番 PC の Python に `python-multipart` / `httpx` がインストール済みか確認（このローカル PC では検証用に追加した）

### Codex の 🔴 #1 修正（ユーザー判断保留中）

- [ ] **`_generate_code` ロジック改善**
  - 現状: Phase1 が「直近100件にない」だけで即 return → 古い既存 code との衝突を DB UNIQUE 例外任せ
  - 影響: 3 桁 1000 件中 800 件以上使用時に 503 が出る確率上昇
  - 現時点では実害なし（1 ユーザ・年 1000 件未満想定なら 3 年は持つ）
  - 修正案: `r.py` の `_generate_code` 関数を以下に置換

```python
def _generate_code(db: Session) -> str:
    used3 = {row[0] for row in db.execute(
        text("SELECT code FROM r_instructions WHERE length(code)=3")).all()}
    if len(used3) < 900:
        for _ in range(50):
            cand = f"{random.randint(100, 999):03d}"
            if cand not in used3:
                return cand
        for n in range(100, 1000):
            cand = f"{n:03d}"
            if cand not in used3:
                return cand
    used4 = {row[0] for row in db.execute(
        text("SELECT code FROM r_instructions WHERE length(code)=4")).all()}
    for _ in range(50):
        cand = f"{random.randint(1000, 9999)}"
        if cand not in used4:
            return cand
    raise HTTPException(503, "code space exhausted")
```

  - ユーザー判断: 1=今すぐ適用 / 2=このまま運用、3 桁が埋まり始めたら改修 / 3=見送り

## 👉 次にやること（優先順）

1. **別 PC（TeamTasks サーバが稼働している PC）でこのプロジェクトを開く**
2. **OneDrive 同期完了を待つ**（本ハンドオフファイル + 014.TeamTasks/* + .claude/commands/r.md 等が同期されること）
3. `/handoff load <ID>` で本セッションの状態を復元（ID は本ファイル冒頭参照）
4. **TeamTasks サーバを再起動**（port 8086、`014.TeamTasks/server/start.bat`）
5. ブラウザで `https://sfuji.f5.si/tasks/r/` を開いて PWA 動作確認（追加・編集・consume）
6. PC 上で `/r` 起動 → 一覧モードと `/r NNN` 単発モードで取り込み動作確認
7. ユーザーに 🔴 #1 修正を適用するか質問する（修正案は上記）

## 🧠 重要な判断と理由

- **判断: TeamTasks 相乗り型を採用**（独立マイクロサービス・JSON ファイル・Discord 拡張案を却下）
  - 理由: 既存の Caddy reverse proxy / FastAPI 基盤 / SQLite を最大流用できる。実装量最小
- **判断: 認証なしで公開**（basic_auth / IP allowlist / VPN を全て不採用）
  - 理由: ユーザー明示判断「A. 認証不要」。社外回線スマホ運用の利便性優先
- **判断: Google Tasks 移行スクリプトは作らない**
  - 理由: ユーザー明示判断「B. 移行不要」。DB は空からスタート
- **判断: 過去指示は削除せず archived フラグで論理削除**
  - 理由: 要件「過去のものは消さずに保存」+ Codex 指摘「編集の監査性」。`r_instruction_versions` で版管理
- **判断: GET は副作用なし、consume を別エンドポイント化**
  - 理由: Codex 🔴 指摘「GET 副作用は API 仕様逸脱・リトライで二重消費」
- **判断: SQLite 同居 + WAL モード**
  - 理由: Codex 🟠 指摘。journal_mode=WAL + busy_timeout=5000 + synchronous=NORMAL で並行性確保

## 📂 触ったファイル

すべて `C:/Users/<本番ユーザー>/OneDrive/デスクトップ/0.フジ/900.ClaudeCode/` 配下:

### 既存ファイル変更（.bak_20260506_191856 でバックアップ済み）
- `014.TeamTasks/server/models.py` — RInstruction / RInstructionVersion クラス 2 つ追加（末尾）
- `014.TeamTasks/server/db.py` — `_enable_sqlite_fk` に PRAGMA 3 行追加、init_db 内の models import に R 系 2 モデル追加
- `014.TeamTasks/server/main.py` — `from routers import ... r as r_router ...` と `app.include_router(r_router.router)` の 2 行
- `014.TeamTasks/server/schemas.py` — 末尾に R 系 5 スキーマ追加
- `.claude/commands/r.md` — 全面書き換え（Google Tasks → DB API）

### 新規ファイル
- `014.TeamTasks/server/routers/r.py` — 7 エンドポイント API（約 240 行）
- `014.TeamTasks/pwa/r/index.html` — Composer / Tabs / Modal の最小 UI
- `014.TeamTasks/pwa/r/style.css` — Google Todo 風スタイル
- `014.TeamTasks/pwa/r/app.js` — fetch ベース SPA、escapeHtml で XSS 防御
- `014.TeamTasks/pwa/r/manifest.json` — PWA manifest（既存 icons 流用）
- `014.TeamTasks/pwa/r/sw.js` — Service Worker（/tasksapi/ はキャッシュ回避）

### リネーム
- `.handoff/r_processed.json` → `.handoff/r_processed.json.frozen_20260506_193016`

## 🔍 動作確認状況

### ローカル PC（jl4lv プロファイル）で実施済み
- [x] 構文チェック: OK（Python ast / FastAPI route 登録）
- [x] import チェック: OK（models / schemas / r.py / db / main 全部）
- [x] パス存在確認: OK（10 ファイル）
- [x] E2E API テスト: OK（TestClient で 23 シナリオ ALL PASS）

### 本番 PC（user プロファイル、TeamTasks サーバ稼働環境）でこれから実施
- [ ] uvicorn 再起動 → 新ルータ反映
- [ ] DB マイグレーション（`init_db` で自動）動作確認
- [ ] PWA `https://sfuji.f5.si/tasks/r/` のブラウザ表示
- [ ] スマホからの追加 → PC `/r NNN` 単発取り込み E2E

## ⚠️ ハマりポイント・注意

1. **OneDrive 同期遅延**: 本ハンドオフを書き終えてから、別 PC が同期完了するまで数分かかる。慌てて load しない
2. **依存パッケージ差異**: ローカル PC（jl4lv）の Python 3.14 には `python-multipart` `httpx` を検証用に追加した。本番 PC が動かなければ `pip install python-multipart httpx fastapi sqlalchemy uvicorn pydantic` で揃える
3. **Caddyfile のパス**: Caddyfile 内で `C:/Users/user/OneDrive/...` とハードコードされている。本番 PC のユーザー名が `user` であることに依存
4. **TeamTasks の WAL モード変更**: 既存 `tasks.db` が rollback journal モードのまま稼働中の場合、再起動で WAL に切り替わる。`tasks.db-wal` `tasks.db-shm` ファイルが新規生成されるが正常動作
5. **`/r` スキル本体の Python urllib**: `006.secretary/scripts/discord_notify.py` `discord_outbox.py` `discord_inbox.py` への import に依存。本番 PC でこれらが動いていることを確認
6. **Caddyfile 設定変更不要**: 既存 `/tasks/*` `/tasksapi/*` ハンドラがそのまま `/tasks/r/*` `/tasksapi/r/*` をルーティングする
7. **server/start.bat に書かれている Python パス**: `C:\\Users\\user\\AppData\\Local\\Python\\pythoncore-3.14-64\\python.exe` — 本番 PC のものなのでそのままで OK

## 💬 次セッションへのメモ

**最初に読むべきは Codex の 🔴 #1**（`_generate_code` 修正案）。ユーザーに「1=今すぐ適用 / 2=後回し / 3=見送り」を聞くこと。

**動作確認の順序**:
1. TeamTasks サーバ再起動 → `https://sfuji.f5.si/health` で 200 確認
2. `https://sfuji.f5.si/tasksapi/r/` を curl/ブラウザで叩いて `[]` （空配列）が返るか確認
3. PWA `https://sfuji.f5.si/tasks/r/` を開いて 1 件追加してみる（番号自動採番）
4. PC で `/r` を実行（一括モード）→ Discord 通知も出るか
5. PWA で別の番号を 1 件追加 → PC で `/r NNN` 単発モード取り込み

**Discord 双方向は温存している**。`.handoff/discord_webhook.txt` `.handoff/discord_channel_id.txt` `.handoff/r_outbox.json` `.handoff/r_response.json` は触っていない。Bot 側 `r_inbox.py` `r_outbox_watcher.py` も無修正。

**旧 Google Tasks リスト「ClaudeCode遠隔指示」**: 経路は廃止したが Google Tasks 上に残っている。手動削除するか別用途で再利用するかは任意。

**E2E が通れば Codex 🔴 #1 を未対応のまま運用開始できる**。3 桁 800 件まで使う頃（数年後）に修正すれば実害なし。

---

**本セッション ID**: `434` ／ **保存日時**: `2026-05-06 23:09`
